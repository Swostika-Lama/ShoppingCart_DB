package org.example;

import javafx.application.Application;


public class Main{
    public static void main(String[] args) {
        // Allow tests to skip launching JavaFX by setting system property 'skip.javafx.launch' to 'true'.
        if (!"true".equals(System.getProperty("skip.javafx.launch"))) {
            Application.launch(ShoppingCartApp.class, args);
        }
    }
}